<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">

    <!-- Show file name in warnings -->
    <module name="TreeWalker">

        <!-- Enforce curly braces on all blocks -->
        <module name="NeedBraces"/>

        <!-- Checks for empty blocks (like empty if or while) -->
        <module name="EmptyBlock"/>

        <!-- Enforce correct naming conventions -->
        <module name="MethodName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="TypeName"/>
        <module name="ConstantName"/>

        <!-- Ensures no magic numbers -->
        <module name="MagicNumber">
            <property name="ignoreNumbers" value="-1,0,1,100"/>
        </module>

        <!-- Checks for unused imports and ordering -->
        <module name="UnusedImports"/>
        <module name="ImportOrder"/>

        <!-- Optional: Disallow empty catch blocks -->
        <module name="EmptyCatchBlock"/>

        <!-- Optional: Force Javadoc on public classes/methods -->
        <!-- <module name="JavadocMethod"/> -->

    </module>

    <!-- Skipped: Indentation check (too strict for most teams) -->
</module>
